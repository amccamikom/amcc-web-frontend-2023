{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "bun run build:vercel",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/api"
        }
    ]
}